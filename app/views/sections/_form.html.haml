= form_for @section do |f|
  -# = error_messages_for @section
  -# - if @section.errors.any?
  -#   #error_explanation
  -#     %h2= "#{pluralize(@section.errors.count, "error")} prohibited this section from being saved:"
  -#     %ul
  -#       - @section.errors.full_messages.each do |message|
  -#         %li= message

  .container.toleft
    .row
      .col-lg-6
        .form-group
          = f.label :kind
          .input-group
            %span.input-group-addon
              = fa_icon "newspaper-o"
            = f.text_field :kind, class: 'form-control'
      .col-lg-6
        .form-group
          = f.label :comment
          .input-group
            %span.input-group-addon
              = fa_icon :commenting
            = f.text_field :comment, class: 'form-control'
    .row
      .col-lg-2
        .form-group
          = f.label :task_id
          .input-group
            %span.input-group-addon
              = fa_icon :book
            = f.select(:task_id,options_for_select(Task.all.pluck('year, id'), f.object.task_id), {},  class: 'form-control selectpicker', "data-live-search" => "true" )
    .actions
      = submit_tag "Создать",  class: "btn btn-primary"
